function sumAll(arr) {
  var min = Math.min(arr[0],arr[1]);
  var max = Math.max(arr[0],arr[1]);
  var result = 0;
  for (var i = min; i <=max; i++){
    result += i;
  }
  return result;
}

console.log(sumAll([1, 4]));
